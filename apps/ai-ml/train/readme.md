## Workflow

1) Setup dataset (move to huggingface if self created the dataset, because of convenience)
2) Setup training in Google colab
3) Download those training files and put them in this dir
4) Upload the train models into huggingface publicly
5) Re-import the models into config.py using huggingface

